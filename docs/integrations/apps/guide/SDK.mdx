# BigCommerce App SDK

The BigCommerce SDK is a simple javascript library which is available as an NPM package instead of our previous approach of providing a CDN link intended to be included on a page as a script tag.

This SDK includes out of the box support for handling logout requests. 
