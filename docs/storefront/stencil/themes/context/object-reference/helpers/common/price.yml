type: object
description: properties common to `product.price` and `product.price.price_range`
properties:
  tax_label:
    type: string
    description: Tax label (VAT, sales tax, etc.) displayed to customers.
  without_tax:
    $ref: https://raw.githubusercontent.com/bigcommerce/docs/main/docs/storefront/stencil/themes/context/object-reference/helpers/common/amount.yml
