type: object
properties:
  store/customer/channel/login/access/updated:
    description: Fires only when you use an API to change the channel access of a customer (`channel_ids` array).
    type: object
    properties:
      producer:
        type: string
      hash:
        type: string
      created_at:
        type: integer
      store_id:
        type: string
      scope:
        type: string
      data:
        type: object
        properties:
          customer_id:
            type: integer
          channel_ids:
            type: array
            items:
              type: integer
