type: object
properties:
  store/priceList/activated:
    description: |
      Fires when you activate a price list, or create one with active:true.
    type: object
    properties:
      producer:
        type: string
      hash:
        type: string
      created_at:
        type: integer
      store_id:
        type: string
      scope:
        type: string
      data:
        type: object
        properties:
          price_list_id:
            type: integer
