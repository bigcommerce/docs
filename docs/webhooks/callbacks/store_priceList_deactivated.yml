type: object
properties:
  store/priceList/deactivated:
    description: | 
      Fires when you deactivate a price list, or create one with active:false.
    type: object
    properties:
      producer:
        type: string
      hash:
        type: string
      created_at:
        type: integer
      store_id:
        type: string
      scope:
        type: string
      data:
        type: object
        properties:
          price_list_id:
            type: integer
