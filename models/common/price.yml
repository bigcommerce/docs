title: Price properties
description: Properties common to `product.price` and `product.price.price_range`.
type: object
properties:
  tax_label:
    type: string
    description: The tax description label displayed to customers. For example, `VAT`, `Sales Tax`, etc.
  without_tax:
    $ref: ./amount.yml
