description: "`{{forms.return}}` object available to `templates/pages/account/add-return.html`"
type: object
properties:
  return:
    type: object
    title: "Return"
    properties:
      actions:
        type: array
        items:
          type: string
      order_id:
        type: string
      order_products:
        type: array
        items:
          type: object
      reasons:
        type: array
        items:
          type: string