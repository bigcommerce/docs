type: object
properties:
  store/option/updated:
    description: Fires when product options overrides are edited. `attributes` and `context` fields are present for only override updates. 
    type: object
    properties:
      scope:
        type: string
      store_id:
        type: string
      data:
        type: object
        properties:
          type:
            type: string
          id:
            type: integer
          affected_product_ids: 
            type: array
            description: 100 `product_id` limit
          context:
            type: object
            properties:
              channel_id: 
                type: integer
              locale:
                type: string
      hash:
        type: string
      created_at:
        type: integer
      producer:
        type: string
